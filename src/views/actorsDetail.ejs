<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Actors</title>
    <link rel='stylesheet' href='/css/style.css' />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" rel="stylesheet" />

</head>
<body>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
    </figure>  
    <div class="btn-group" role="group" aria-label="Basic example">
        <a href="/actors" class="btn btn-primary"><i class="fa fa-home" aria-hidden="true"></i> Actors Home</a>
      </div>  
    <h1><%= actor.name %> </h1>
    <br>
    <p>NAME: <%= actor.last_name+" "+actor.first_name %> </p>
    <p>RANKIG: <%= actor.rating %> <%- (actor.rating>4)?'<i class="fa-solid fa-thumbs-up"></i>':'<i class="fa-solid fa-thumbs-down"></i>' %>
        <% if (actor.movies && actor.movies.length) { %>
            <div class="alert alert-primary">
                <label>Pelicula Favorita:
                    <b>"<%= actor.movies.title %>"</b>
                </label>
            </div>
        <%}else{%>
            <div class="alert alert-warning">No se encontraron Películas Favoritas</div>
        <% } %>
</p>
    <h3>Películas en las que trabajo</h3>
    <% if (actor.moviesAll && actor.moviesAll.length > 0) { %>
        <div class="alert alert-primary">
            <ul>
                <% actor.moviesAll.forEach(movies => { %>
                    <li><%= movies.title %></li>
                <% }); %>
            </ul>
        </div>
    <% } else { %>
        <div class="alert alert-warning">No se encontraron actores para esta película</div>
    <% } %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>